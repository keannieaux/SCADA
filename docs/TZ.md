# Техническое задание: SCADA-система

**Статус:** черновик v1.4
**Дата:** 2026-08-14
**Репозиторий:** `C:\Users\Hycaidzee\RiderProjects\SCADA`

Документ описывает требования, архитектуру и план работ. Является основным
источником истины для всех участников разработки. При расхождении кода и ТЗ —
сначала обновляется ТЗ, потом код.

---

## 1. Назначение

Десктопная SCADA/HMI-система для промышленного объекта: сбор данных с
программируемых логических контроллеров, отображение технологического процесса
на мнемосхемах, тренды, сигнализация, архивирование и управление.

### 1.1 Ключевые ограничения

| Ограничение | Значение |
|---|---|
| Целевое железо (АРМ оператора) | Слабый производственный ПК: 2 ядра x64 ~2 ГГц, 4 ГБ RAM, встроенная графика |
| Целевое железо (мастер-АРМ / сервер) | Производственный ПК: 4 ядра x64, 8 ГБ RAM, SSD |
| ОС | Windows 10/11 x64 (архитектура кроссплатформенная, но целимся в Windows) |
| Количество тегов | **10 000 (подтверждено заказчиком), проектируем с запасом до 20 000** |
| Логируемые теги | Не все 10 000; реально в архив пишется 10–30 % тегов. Система должна стабильно опрашивать 10 000+ тегов |
| Глубина архива | **Настраивается** (`RetentionDays`, §8.6). Для текущего проекта — 1 год. Формат ограничений на срок не накладывает |
| Контроллеры | LicOS (Unionscience Technology Group), серии EC200/EC300/EC400, MC500/MC600 |
| Редактор мнемосхем | Обязателен, встроен в систему |
| Рабочих мест | Несколько — клиент-серверная архитектура принята как базовая (§5.1, `docs/distributed-arm-architecture.md`) |

### 1.2 Приоритеты

1. **Производительность на слабом железе** — главный критерий.
2. **Чистота архитектуры и читаемость кода** — система будет жить годами.
3. Функциональная полнота.

Именно в этом порядке. Если фича требует пожертвовать производительностью или
чистотой слоёв — фича переделывается или откладывается.

---

## 2. Термины

| Термин | Определение |
|---|---|
| **Тег** (Tag) | Именованная единица данных: значение + метка времени + качество. Аналог «канала» в RapidScada. |
| **Качество** (Quality) | Достоверность значения: Good / Uncertain / Bad. |
| **Драйвер** | Модуль обмена по конкретному протоколу (Modbus, OPC UA). |
| **Устройство** (Device) | Логический контроллер, опрашиваемый драйвером. |
| **Канал связи** (Channel) | Физическое соединение (TCP-сокет, COM-порт), обслуживает одно или несколько устройств. |
| **Мнемосхема** (Scheme) | Векторное изображение техпроцесса с элементами, привязанными к тегам. |
| **Динамизация** | Правило «свойство фигуры зависит от значения тега». |
| **Поток** (Stream) | Последовательность значений одного тега в архиве. Единица хранения и адресации. См. `docs/archive-format.md` §4. |
| **Решётка значения** | Множество значений, физически достижимых для тега: `raw × ScaleFactor + ScaleOffset`. Основание для хранения целого вместо `double` (§8.2). |
| **Квитирование** | Подтверждение оператором факта ознакомления с аварийным сообщением. |
| **Инженерная поставка** | Дистрибутив с редактором и компилятором. Предназначен разработчикам SCADA. См. §5.4. |
| **Исполнительная поставка** | Дистрибутив без инструментальных средств. Передаётся заказчику. См. §5.4. |
| **Пакет** (`.scadapkg`) | Скомпилированный проект: бинарный, с раннесвязанными тегами и байткодом выражений. См. §14.2. |
| **Выражение** | Формула динамизации, компилируемая в байткод стековой ВМ. См. §11.2. |
| **Раннее связывание** | Замена имени тега на индекс в `TagTable` на этапе компиляции пакета. См. §11.6. |
| **Realtime-БД** | Оперативное хранилище текущих значений всех тегов в памяти. |

---

## 3. Технологический стек

| Компонент | Выбор | Обоснование |
|---|---|---|
| Платформа | .NET 10 | Установлен SDK 10.0.302 |
| UI-фреймворк | Avalonia 12.1.1 | Кроссплатформенность, контроль над рендером |
| Графика | SkiaSharp 3.119 | Прямой доступ к канве, обход дерева контролов |
| MVVM | CommunityToolkit.Mvvm 8.4.2 | Генераторы кода вместо рефлексии |
| DI / хостинг | Microsoft.Extensions.Hosting | Стандарт, фоновые службы из коробки |
| Графики | **ScottPlot 5** | Быстрее LiveCharts2 на слабом железе, `DataStreamer` для realtime |
| SVG | Svg.Skia | Готовый рендер SVG-символов в Skia |
| Архив значений | **Собственный бинарный формат** | Запрос всегда один — «поток за диапазон», SQL не нужен. Ноль установки и администрирования на объекте. См. §8.7 и `docs/archive-format.md` |
| События, алармы, аудит, пользователи | **SQLite** | Реляционные, фильтруемые, транзакционные данные малого объёма. Встраиваемый, без сервера и администрирования |
| Сериализация | System.Text.Json + source generators | Без рефлексии, AOT-совместимо |
| Тесты | xUnit | — |
| Бенчмарки | BenchmarkDotNet | Контроль регрессий производительности |

### 3.1 Отклонённые варианты

- **LiveCharts2** — красивее и с анимациями, но больше аллокаций и нагрузка на GC.
  Оставлен как запасной, если ScottPlot не устроит визуально.
- **PostgreSQL + TimescaleDB для архива значений** — принят в v1.3, отклонён в
  v1.4. Три причины. Первая: запрос к архиву всегда один и тот же — «этот тег за
  этот диапазон», SQL для него избыточен. Вторая: 150–300 МБ RAM из бюджета
  500 МБ на АРМ оператора (§4.1) при том, что после пересмотра §8 весь годовой
  архив укладывается в десятки гигабайт файлов. Третья: установка, обновление,
  бэкап и администрирование сервера БД на промышленном ПК, где нет
  администратора. Возможность возврата сохранена: `IArchiveStore`
  (`docs/archive-format.md` §13.3) допускает реализацию на TimescaleDB без
  переделки конвейера, а conformance-тесты не дают шву выродиться.
- **Единый процесс (ядро и HMI вместе, `LocalRuntimeClient`)** — принят в v1.0,
  отклонён при пересмотре в v1.4: выбрана клиент-серверная архитектура с
  раздельными сервером сбора и клиентом HMI. См. §5.1 и
  `docs/distributed-arm-architecture.md`.

---

## 4. Нефункциональные требования

Это **критерии приёмки**, а не пожелания. Проверяются замерами на целевом железе.

### 4.1 Производительность

| Показатель | Требование |
|---|---|
| Запуск приложения до рабочего окна | ≤ 5 с |
| Потребление RAM, 10 000 тегов, установившийся режим (АРМ оператора) | ≤ 500 МБ |
| Потребление RAM, 10 000 тегов (мастер-АРМ: опрос + архив + клиенты) | ≤ 800 МБ |
| Загрузка CPU: опрос 10 000 тегов @ 1 Гц + открытая мнемосхема | ≤ 25 % на 2 ядрах |
| Частота кадров мнемосхемы, 500 динамических элементов | ≥ 30 FPS |
| Задержка «значение принято → отображено» | ≤ 200 мс |
| Открытие тренда: 10 тегов за 8 часов | ≤ 1 с |
| Открытие тренда: 10 тегов за год | ≤ 2 с |
| Отклик UI при переключении страниц | ≤ 100 мс |
| Пропускная способность `TagTable` на запись | ≥ 1 000 000 значений/с (один поток) |

### 4.2 Надёжность

- Обрыв связи с устройством **не влияет** на работу UI и остальных устройств.
- Автоматическое переподключение с экспоненциальной задержкой (1с → 30с, потолок).
- При потере связи теги устройства получают качество `Bad`, значение сохраняется
  последнее известное с исходной меткой времени.
- Аварийное завершение не приводит к потере архива более чем за последние 10 секунд
  (периодический flush).
- Повреждённый файл архива не блокирует работу — пропускается с записью в лог.

### 4.3 Ресурсы диска

Требование к диску — не константа продукта, а **расчёт на проект**: объём
зависит от числа логируемых тегов, частоты записи и глубины хранения (§8.6),
которые у каждого заказчика свои.

```
требуемое место ≈ расчёт калькулятора × 3
```

Тройной запас — потому что константы калькулятора выведены из замера на
модельном сигнале, а форма реального техпроцесса от него отличается. Для
текущего проекта (2000 логируемых тегов, 400 суток): расчёт ~24 ГБ, требование
к диску — **100 ГБ**.

**Калькулятор проверен суточным прогоном** (тест
`ArchiveVolumeCalculatorTests.DailyRun_ActualGrowthMatchesEstimate`):
фактический рост архива расходится с предсказанием на **9 %** при допуске 25 %.
Это же и есть критерий приёмки M4 в исполняемом виде.

Требование к системе: `Scada.Package.Builder` вычисляет ожидаемый рост архива по
фактической конфигурации и предупреждает при сборке пакета. Это даёт два
эффекта: требование к оборудованию выдвигается заказчику **на этапе
проектирования**, а не при заполнении диска, и отсекается единственный реальный
способ исчерпать место — залогировать все теги на максимальной частоте, что даёт
порядка терабайта в год.

---

## 5. Архитектура

### 5.1 Главное решение

> **Система строится по клиент-серверной схеме: сервер сбора данных и HMI — раздельные компоненты.**

- **Сервер** — headless-процесс (консоль / Windows Service): опрос контроллеров,
  `TagTable`, архив, сигнализация, обслуживание клиентских подключений по gRPC.
- **Клиент** — Avalonia-приложение оператора: мнемосхемы, тренды, сигнализация,
  управление. Подключается к серверу по сети.

Один ПК может выполнять обе роли (single ARM / мастер-АРМ); сервер может быть
вынесен на отдельную машину без изменения кода. Режимы развёртывания и обоснование
выбора — в `docs/distributed-arm-architecture.md`, который является частью ТЗ.

Взаимодействие клиента с сервером идёт через единственный узкий контракт —
gRPC-сервис. Принципы: сервер не знает про UI, клиент не знает про PLC и
конкретные протоколы.

### 5.2 Структура решения

```
SCADA.sln
├─ Directory.Build.props          общие настройки сборки
├─ Directory.Packages.props       Central Package Management
├─ .editorconfig                  стиль кода
├─ CLAUDE.md                      правила проекта для ИИ-ассистента
├─ docs/
│  └─ TZ.md                       этот документ
├─ src/
│  ├─ Scada.Core                  примитивы и интерфейсы. Ноль зависимостей.
│  ├─ Scada.Expressions           байткод-ВМ выражений (§11). Только исполнение.
│  ├─ Scada.Runtime               TagTable, движок опроса, планировщик, IRuntimeClient
│  ├─ Scada.Drivers.Abstractions  IDeviceDriver, IChannel, модель конфигурации драйвера
│  ├─ Scada.Drivers.Simulator     генератор данных для разработки и демо
│  ├─ Scada.Drivers.Modbus        Modbus TCP и RTU
│  ├─ Scada.Historian             архив значений: конвейер, кодеки, стор, ротация
│  ├─ Scada.Alarms                правила, состояния, квитирование, журнал
│  ├─ Scada.Graphics              модель мнемосхемы + Skia-рендерер (без Avalonia)
│  ├─ Scada.Package               чтение пакета .scadapkg (§14.2)
│  │
│  │  ── только инженерная поставка (§5.4) ──────────────────
│  ├─ Scada.Expressions.Compiler  парсер и компилятор выражений в байткод
│  ├─ Scada.Package.Builder       сборка проекта в .scadapkg, валидация
│  ├─ Scada.Editor                редактор мнемосхем
│  │
│  │  ── приложения ──────────────────────────────────────────
│  ├─ Scada.App.Shared            shell, навигация, страницы просмотра
│  ├─ Scada.App.Runtime           ИСПОЛНИТЕЛЬНАЯ поставка (заказчику)
│  └─ Scada.App.Engineering       ИНЖЕНЕРНАЯ поставка (разработчикам)
├─ tests/
│  ├─ Scada.Core.Tests
│  ├─ Scada.Expressions.Tests
│  ├─ Scada.Runtime.Tests
│  ├─ Scada.Historian.Tests
│  ├─ Scada.Package.Tests
│  ├─ Scada.Drivers.Modbus.Tests
│  └─ Scada.Architecture.Tests    проверка правил зависимостей
└─ bench/
   └─ Scada.Benchmarks            BenchmarkDotNet
```

### 5.3 Правило зависимостей

Зависимости направлены **строго вниз**:

```
Scada.App.Runtime          Scada.App.Engineering
        ↓                            ↓
        └──── Scada.App.Shared ──────┘
                     ↓                ↓ (только Engineering)
Scada.Graphics / Scada.Alarms /   Scada.Editor
Scada.Package                     Scada.Expressions.Compiler
                     ↓                Scada.Package.Builder
              Scada.Runtime
                     ↓
Scada.Drivers.Abstractions / Scada.Historian
                     ↓
        Scada.Core / Scada.Expressions
```

**`Scada.Historian` находится ниже `Scada.Runtime`, а не выше.** В редакции
v1.2 предполагалось обратное — что архив будет высокоуровневым модулем над
рантаймом. По факту реализации M4 сложилось иначе и лучше:

- `Scada.Historian` — механика хранения: кодеки, блоки, файлы, журнал. Она не
  знает ни про `TagTable`, ни про конфигурацию проекта, поэтому её тесты идут
  без рантайма, а формат можно проверять и мерить изолированно.
- Интеграция с рантаймом — конвейер, реестр потоков, фасад `IHistorian`,
  диагностика — живёт в `Scada.Runtime`, потому что ей нужны и `TagTable`,
  и конфигурация.

Разделение проходит по границе «формат против интеграции», а не «низкое против
высокого», и это оказалось полезнее исходной схемы.

Жёсткие запреты:

- Приложения **не знают** о существовании Modbus, OPC UA и любого конкретного протокола.
- `Scada.App.Runtime` **не ссылается** на `Scada.Editor`,
  `Scada.Expressions.Compiler` и `Scada.Package.Builder`. Это ключевое требование
  §5.4: инструментальный код физически отсутствует в поставке заказчику.
- `Scada.Graphics` **не ссылается на Avalonia** — только на SkiaSharp. Модель схемы и
  рендерер должны работать в консольном приложении (для будущей серверной генерации
  изображений и для тестов).
- `Scada.Expressions` содержит **только исполнение** байткода. Парсер и компилятор —
  в отдельной сборке `Scada.Expressions.Compiler`, в исполнительную поставку не входит.
- `Scada.Core` не ссылается ни на что, кроме BCL.
- Конкретные драйверы (`Scada.Drivers.*`) не ссылаются друг на друга.
- **`Scada.Runtime` не ссылается ни на один конкретный драйвер.** Драйверы
  регистрируются в composition root приложения (`DriverFactory.Register`), а не
  вкомпилированы в рантайм. Это то же правило, что держит вне поставки Modbus и
  OPC UA, и относится оно в том числе к симулятору: он инструмент разработки, и
  в дистрибутиве заказчика ему делать нечего.

Правила проверяются автотестом в `Scada.Architecture.Tests`. Это не бюрократия:
без автопроверки слои размываются за пару месяцев. Для запрета на
инструментальные сборки в `Scada.App.Runtime` автопроверка обязательна — нарушение
этого правила обнаружится только при передаче системы заказчику.

**Известное нарушение на 2026-08-14:** `Scada.Runtime` ссылается на
`Scada.Drivers.Simulator` — `DriverFactory` регистрирует симулятор по
умолчанию. Обнаружено при разборе M4, потому что автотеста нет. Устраняется
переносом регистрации в composition root.

---

### 5.4 Состав поставки: инженерная и исполнительная

Система поставляется **двумя раздельными дистрибутивами из одной кодовой базы**.

| | **Инженерная поставка** `Scada.App.Engineering` | **Исполнительная поставка** `Scada.App.Runtime` |
|---|---|---|
| Получатель | Разработчики SCADA (мы), интеграторы | Заказчик, операторские станции |
| Редактор мнемосхем | Есть | **Нет** |
| Компилятор выражений | Есть | Нет (только исполнение байткода) |
| Сборка пакета `.scadapkg` | Есть | Нет |
| Конфигурация тегов и устройств | Редактирование | Только просмотр |
| Работа с исходным проектом | Да | Нет |
| Загрузка `.scadapkg` | Да | Да |
| Сбор данных, схемы, тренды, сигнализация | Да | Да |
| Диагностика каналов и тегов | Да | **Да, обязательно** (§5.4.2) |

#### 5.4.1 Обоснование

Разделение инструментальной и исполнительной систем — отраслевая норма
(WinCC Engineering / Runtime, InTouch WindowMaker / WindowViewer,
Ignition Designer / Client, RapidScada Administrator / Server).

Рассмотренные и отклонённые альтернативы:

| Вариант | Причина отклонения |
|---|---|
| Единый бинарник, редактор скрыт флагом или лицензией | Код редактора физически поставляется заказчику: лишний вес, время старта и поверхность атаки. Флаг обходится тривиально. Бюджеты §4.1 расходуются на код, который никогда не выполняется. |
| Редактор отдельным продуктом и репозиторием | Дублирование модели схем и формата файлов, неизбежная рассинхронизация, двойная поддержка. Недопустимо при двух разработчиках. |
| Редактор как догружаемый плагин (`AssemblyLoadContext`) | Требует инфраструктуры версионирования плагина против хоста ради выгоды, которую разделение сборок даёт бесплатно. Оправдан только при планах на стороннюю экосистему расширений — такого требования нет. |

Выбранный вариант даёт три эффекта: у заказчика отсутствует инструментальный код,
дистрибутив меньше и стартует быстрее, а невозможность обратиться к редактору из
исполнительной части обеспечивается компилятором, а не дисциплиной.

#### 5.4.2 Следствия

- **Диагностика в исполнительной поставке обязательна.** Раз редактора на объекте
  нет, разбор нештатной ситуации ведётся средствами runtime: состояние каналов
  (§7.4), живые значения тегов, журналы, просмотр состава загруженного пакета.
  Это требование, а не побочный эффект.
- **Общий код выносится в `Scada.App.Shared`.** Дублирование страниц между двумя
  приложениями недопустимо.
- **Решение принимается на M0.** Расплетание двух поставок на M6, когда редактор
  уже написан внутри общего приложения, несоизмеримо дороже.

---

## 6. Модель данных

### 6.1 Значение тега

Основной тип, от которого зависит вся производительность системы.

```csharp
public readonly record struct TagId(int Value);

public enum Quality : byte
{
    Bad       = 0,
    Uncertain = 64,
    Good      = 192
}

public readonly record struct TagValue(
    double  Value,          // 8 байт
    long    TimeStampUtc,   // 8 байт, Unix-миллисекунды UTC
    Quality Quality);       // 1 байт
```

Приведено в соответствие с кодом (`SCADA.Core/Tags/TagValue.cs`). Два отличия от
редакции v1.3, зафиксированные по факту реализации:

- **Метка времени — Unix-миллисекунды, а не тики.** Совпадает с единицей времени
  архива, преобразование при записи не требуется. Разрешение 1 мс избыточно при
  максимальной частоте опроса 10 Гц.
- **Поле `TagFlags` отсутствует** — потребности в нём не возникло.

Требования к модели:

- **Структура, не класс.** ~24 байта, размещается в массиве, не грузит GC.
- **Хранение в плоском массиве** `TagValue[]`, индекс = `TagId`. Доступ O(1)
  без хеширования. Словарь `имя → TagId` используется **только при загрузке
  конфигурации**, в горячем пути запрещён.
- **Никакого `INotifyPropertyChanged` на теге.** 10 000 объектов с событиями
  на каждое изменение — гарантированная смерть производительности.
- Дискретные значения хранятся в том же `double` (0.0 / 1.0). Отдельного типа нет —
  это упрощает архив, тренды и алармы.
- Строковые теги (редкие, < 5 % в типичной SCADA) — в отдельной таблице
  `StringTagTable` с собственной индексацией.

### 6.2 Описание тега (конфигурация)

Загружается при старте, в рантайме не меняется (изменение = перезагрузка конфигурации):

```
TagDefinition:
  Id            TagId
  Name          string      уникальное имя, напр. "Boiler1.Temp"
  Description   string
  Units         string      "°C", "бар"
  DataType      enum        Analog | Discrete | String
  DeviceId      int
  Address       string      адрес в терминах драйвера
  ScaleFactor   double      линейное преобразование
  ScaleOffset   double
  MinValue      double      границы для трендов и валидации
  MaxValue      double
  IsArchived    bool        пишется ли тег в архив вообще (§8.3)
  Logging       {...}       режим логирования: Periodic | OnChange | Schedule (§8.3)
  Precision     int?        число значащих разрядов для тегов без решётки (§8.2)
  IsWritable    bool
```

Отдельной настройки архивирования на тег, кроме этих, **нет**: режим логирования
по умолчанию выводится из `DataType`, представление значения — из `ScaleFactor`,
глубина хранения одна на систему. Инженер переопределяет режим только там, где
умолчание не подходит.

`IsArchived` вынесен в явный флаг, а не выражается отсутствием `Logging`: при
10 000 тегов, из которых логируется 10–30 % (§1.1), это самая частая настройка
проекта, и она должна быть видимой и проверяемой при сборке пакета.

`Precision` применяется только к тегам, у которых нет целочисленной решётки
(вычисляемые, настоящий float из ПЛК). Округление до объявленной точности —
**с потерями**, поэтому только по явному указанию инженера.

---

## 7. Подсистема сбора данных

### 7.1 Контроллеры LicOS

По документации производителя серии EC300/EC400/MC500/MC600 поддерживают:
Modbus, Profibus, EtherNet/IP, Profinet, CANopen, EtherCAT, OPC UA, MQTT.

**Для SCADA релевантны только два:**

- **Modbus TCP/RTU** — основной. Универсален, есть у всех серий, предсказуем
  по нагрузке, прост в отладке.
- **OPC UA** — если потребуется доступ к тегам по именам с типами и метаданными.
  Дороже по CPU и памяти.

EtherCAT, Profinet, CANopen — это **полевые шины между ПЛК и модулями ввода-вывода**.
SCADA по ним не работает: мы общаемся с самим контроллером, а он опрашивает
периферию. MQTT — опционально, при наличии брокера у заказчика.

### 7.2 Драйверы как плагины

```csharp
public interface IDeviceDriver
{
    string ProtocolName { get; }
    Task ConnectAsync(CancellationToken ct);
    Task<PollResult> PollAsync(ReadOnlySpan<TagAddress> addresses, Span<TagValue> results, CancellationToken ct);
    Task WriteAsync(TagAddress address, double value, CancellationToken ct);
    Task DisconnectAsync();
}
```

Добавление нового протокола = новый проект `Scada.Drivers.<Name>`, регистрация
в DI. Правок в остальном коде — ноль.

### 7.3 Требования к циклу опроса

- **Ноль аллокаций в установившемся режиме.** `ArrayPool<byte>` для буферов кадров,
  `Span<byte>` для парсинга, переиспользуемые массивы результатов.
- `PeriodicTimer` вместо `while (true) { await Task.Delay(...) }` — не накапливает дрейф.
- Один канал связи = один `Task`. Устройства на одном COM-порту опрашиваются
  последовательно, устройства на разных TCP-соединениях — параллельно.
- **Частота опроса настраивается на устройство**, не глобально. Аварийные сигналы —
  10 Гц, температуры — раз в 5 секунд. Это главный рычаг снижения нагрузки.
- **Частота опроса ≠ частота записи в архив** (см. §8.3).
- Группировка запросов: соседние регистры Modbus читаются одним запросом
  (оптимизатор объединяет адреса в непрерывные блоки при загрузке конфигурации).
- Все операции ввода-вывода асинхронные, с `CancellationToken` и таймаутом.

### 7.4 Диагностика канала

Каждый канал публикует собственные теги диагностики (доступны на мнемосхемах
и в трендах наравне с технологическими):

- состояние соединения, время последнего успешного обмена;
- количество успешных/неуспешных запросов, число переподключений;
- среднее и максимальное время отклика.

### 7.5 Диагностика архива

Архив публикует собственные диагностические теги тем же механизмом и с теми же
правами доступа, что каналы связи. Требование вытекает из §5.4.2: редактора на
объекте нет, и вопросы «за какой период у меня вообще есть данные» и «пишется ли
архив прямо сейчас» должны иметь ответ в runtime.

Состав — `docs/archive-format.md` §22. Обязательный минимум: фактическая глубина
архива, состояние записи, свободное место и прогноз на сколько его хватит,
счётчики отброшенных и потерянных отсчётов.

---

## 8. Историзация

Требование заказчика — «архив за год». Раздел определяет, какими механизмами оно
выполняется. Полная спецификация формата — в `docs/archive-format.md`, который
является частью настоящего ТЗ.

### 8.1 Порядок величин

Если бы в архив писались **все 10 000 тегов** каждую секунду без сжатия:

```
10 000 тегов × 86 400 с × 16 байт = 13,8 ГБ в сутки → ~5 ТБ в год
```

Так делать нельзя, но и трёхэтажной конструкции задача не требует. В архив
пишется **10–30 %** тегов (§1.1), а на сжатом представлении год сырых значений
для 2000 тегов укладывается в **десятки гигабайт** (§8.5). Отсюда главное
решение раздела:

> **Хранится один уровень — сырые значения — за весь срок хранения.
> Уровней агрегации нет.**

Агрегаты нужны не для экономии места, а для скорости чтения широких диапазонов,
и в выбранном формате они получаются из заголовков блоков ценой **ноль байт**
(§8.4).

Работают три механизма.

### 8.2 Механизм 1: представление значения

Значение аналогового тега приходит из целого регистра ПЛК:
`value = raw × ScaleFactor + ScaleOffset`. Достижимые значения образуют решётку с
шагом `ScaleFactor`, поэтому в архив пишется **целое `raw`, а не `double`**.
Преобразование обратимо бит в бит — потерь нет.

Выигрыш в том, что целые кодируются разностью разностей, которая на равномерно
меняющихся сигналах вырождается в ноль: стабильный тег, линейный разгон и
счётчик-накопитель стоят **1 бит на значение**. XOR по `double` на тех же данных
стоит 12–20 бит.

Кодек выбирается автоматически, на каждый блок, проверкой решётки по факту:
если хоть одно значение блока не восстанавливается точно — блок пишется XOR-ом
по `double`. Настройки на тег нет, `ScaleFactor` работает как подсказка, а
корректность проверяется. Для вычисляемых тегов и настоящих float допустимо
округление до объявленной точности — с потерями, только по явному указанию.

**Пороговый отбор (deadband) не применяется.** Он и сжатие расходуют одну и ту
же избыточность: после отбора метки времени становятся нерегулярными, а повтор
значения не наступает по определению, то есть выключаются оба однобитных случая
кодека. Итоговый выигрыш падает примерно до 1,2× ценой безвозвратной потери
данных и настройки порога на каждом из тысяч тегов. Возврат дёшев и формат не
меняет: это фильтр в конвейере.

По той же причине не нужен **heartbeat** — он существовал, чтобы вернуть
регулярность, сломанную пороговым отбором.

### 8.3 Механизм 2: режим логирования

Определяет, **когда** значение попадает в архив. Выводится из `DataType`,
настройка на тег не требуется:

| Режим | По умолчанию для | Что пишется |
|---|---|---|
| **Periodic** | `Analog` | Отсчёт через фиксированный интервал, независимо от изменения |
| **OnChange** | `Discrete` | Отсчёт при изменении значения либо качества |
| **Schedule** | только явно | Отсчёт в заданные моменты: смена, сутки, месяц. Отчётные срезы |

Интервал Periodic по умолчанию — `max(период опроса, 1 с)`. Это единственный
рычаг снижения объёма, и он **предсказуем**: объём вычисляется из конфигурации
точно. Пороговый отбор такого свойства не даёт — при нём объём зависит от
поведения процесса и растёт именно в аварию, когда дрожат все теги, то есть диск
заполняется в день, когда архив нужнее всего.

Дискретный тег при OnChange стоит в сотни раз дешевле, чем при Periodic: писать
его 86 400 раз в сутки ради 6 переключений бессмысленно даже по 2 бита.

**Пропуск отсчётов означает разное в разных режимах:** при Periodic — «не
собирали» (тренд разрывается), при OnChange — «не менялось» (значение
удерживается). Поэтому режим хранится в заголовке каждого блока, а не только в
конфигурации.

### 8.4 Механизм 3: сжатие потока

Разность разностей для меток времени, разность разностей либо XOR для значений
(схема Gorilla). Данные нарезаны на блоки по 4096 отсчётов, каждый блок несёт
**summary-заголовок**: `min`, `max`, сумма, `count`, число достоверных отсчётов,
границы времени.

Заголовок даёт два эффекта сразу. Он позволяет читать широкий диапазон, вообще
не разжимая содержимое — год по одному тегу это около 7700 заголовков вместо
30 миллионов точек. И он делает ненужным отдельный уровень минутных агрегатов:
при накладных расходах 3–5 % от объёма блока он выполняет ту же работу, тогда
как минутный агрегат стоит фиксированные 32 байта на тег в минуту независимо от
активности — то есть на данных SCADA дороже сырья, которое обобщает.

Качество (`Good` / `Uncertain` / `Bad`) хранится обязательно, длинами серий:
без него период с оборванной связью неотличим от нормальной работы.

**В архив пишется только достоверное.** При переходе качества в `Uncertain` или
`Bad` записывается одна точка с новым качеством — чтобы на тренде было видно,
когда именно оборвалось, — и далее не пишется ничего до восстановления.
Образующийся пропуск в Periodic-потоке по §8.3 и означает «не собирали». Иначе
трёхчасовой обрыв дал бы 10 800 копий устаревшего значения, утверждающих, что
измерение велось.

**Метки времени внутри потока строго возрастают** — этого требуют и кодек, и
поиск по диапазону. Отсчёт с меткой не позже последней записанной отбрасывается
со счётчиком в диагностику (§7.5); скачок часов вперёд закрывает блок.
Требование не теоретическое: `PollingEngine.MarkTagsBad` обновляет метку
времени при неизменном значении на каждой неудачной попытке переподключения.

### 8.5 Оценка объёма

Смесь: 2000 логируемых тегов, 1500 аналоговых при Periodic 1 Гц и 500 дискретных
при OnChange.

```
Аналоговый тег: ~4 бита на отсчёт + заголовки блоков  ≈ 16 МБ на тег в год
    1500 тегов                                        ≈ 24 ГБ в год
Дискретный тег при OnChange, ~50 переключений в сутки ≈ 55 КБ на тег в год
    500 тегов                                         ≈ 0,03 ГБ в год
                                                      ─────────────────
                                                      ≈ 24 ГБ в год
```

Оценка получена расчётом, а не замером, разброс **2–3 раза**: доля
неизменившихся отсчётов зависит от реального техпроцесса. Отсюда требование к
диску в §4.3 с трёхкратным запасом и самосогласованный критерий приёмки M4
(§17): фактический рост сравнивается не с этим числом, а с предсказанием
калькулятора объёма по фактической конфигурации.

### 8.6 Глубина хранения

Глубина — **параметр проекта, а не свойство системы**. Система разрабатывается
как переиспользуемый продукт: у следующего заказчика срок может быть два года
или пять, и это не должно требовать изменений в коде.

Формат ограничений на срок не накладывает (`docs/archive-format.md` §15): место
растёт линейно и предсказуемо (~24 ГБ на год при 2000 логируемых тегов),
требование к диску выдвигается заказчику на этапе проектирования по расчёту
калькулятора объёма (§4.3).

Конфигурация M4: `RetentionDays`, по умолчанию **400**. Ротация — удаление
каталогов месяцев, целиком вышедших за срок.

Два требования на будущее, оба выполняются без изменения формата:

- **Срок разрешается на поток через `IRetentionPolicy`**, даже пока ответ всегда
  один. Глубина на класс тегов («аварийные три года, вспомогательные полгода») —
  предсказуемое требование следующих проектов, а узел стоит пять строк.
  Сущность «группа архивирования» заводится тогда, когда появится заказчик,
  которому она нужна, а не заранее.
- **При глубине более 2–3 лет вводится часовая свёртка** — единственный случай,
  когда уровень агрегации оправдан. Оправдан он скоростью чтения широких
  диапазонов, а не экономией места: на пяти годах чтение по summary-заголовкам
  выходит на предел бюджета §4.1.

Политик на группу тегов в M4 нет. Группы в редакции v1.3 задавали глубину
уровней хранения; уровень один, режим логирования выводится из `DataType`, и
единственное, что могло бы различаться по группам, — срок, который вынесен в
`IRetentionPolicy`.

Заморозка данных вокруг аварий не требуется ни при какой глубине: в пределах
срока хранения не удаляется ничего, замораживать нечего.

### 8.7 Формат хранения

- **Значения:** собственный append-only бинарный формат, файл на поток на месяц.
  Спецификация — `docs/archive-format.md`.
- **События, алармы, аудит, пользователи:** SQLite. Это реляционные,
  фильтруемые, транзакционные данные малого объёма — тысячи записей в сутки, а
  не миллионы. Собственный формат для них был бы ошибкой, сервер БД — избыточен.
- **PostgreSQL + TimescaleDB:** не реализуется (обоснование в §3.1). Возможность
  сохранена: шов `IArchiveStore` (`docs/archive-format.md` §13.3) допускает такую
  реализацию без переделки конвейера.

Ни одного сервера БД на объекте. Требование к развёртыванию: копирование
каталога, без установки и администрирования.

### 8.8 Сроки проектирования

Формат проектируется **на бумаге до начала реализации** и совместно (§16.7).
Реализуется в M4. Причина: переделка хранилища после накопления реальных данных
крайне болезненна.

Выполнено: `docs/archive-format.md`, v1.0 от 2026-08-14. В M1 требование
выполнено не было — долг закрыт на входе в M4.

### 8.9 Поведение при нехватке места на диске

Заполнение диска обрабатывается как штатная ситуация и **не влияет на опрос и
HMI** — по тому же принципу, что обрыв связи (§4.2).

Принцип: потерять новые данные хуже, чем потерять старые. Инцидент, который
будут разбирать, происходит сейчас, и остановка записи при заполнении диска
гарантирует отсутствие записей именно про него. Но и молча срезать обещанный
заказчику срок нельзя. Отсюда лестница с полом:

1. Свободно меньше `MinFreeDiskMb` — **аларм**, запись продолжается.
2. Место кончается, `OnDiskFull = DeleteOldest` — удаляется самое старое, но не
   глубже `MinRetentionDays`; каждое такое удаление в журнал аудита.
3. `OnDiskFull = StopWriting` либо пол `MinRetentionDays` больше не удержать —
   запись архива прекращается, опрос и HMI работают, аларм, счётчик потерянных
   отсчётов растёт.

`MinRetentionDays` — договорный минимум. Если срок хранения жёсткий, интегратор
приравнивает его к `RetentionDays`, и досрочного удаления не будет никогда.

Ни один из режимов не молчит: тихая потеря данных недопустима — о ней узнают
через полгода, когда данные понадобятся.

Полный перечень параметров архива — `docs/archive-format.md` §21.

### 8.10 Требования к реализации

Вытекают из §4.2 и обязательны к соблюдению:

- **Журнал упреждающей записи** со сбросом на диск не реже 10 с — потеря при
  аварийном завершении не превышает 10 секунд.
- **CRC на каждый блок**, обрезка порванного хвоста при открытии, изоляция
  повреждения: теряется не более одного тега за один месяц.
- **Поток идентифицируется именем тега, а не `TagId`.** `TagId` приходит из
  пакета и у диагностических тегов выводится из порядка каналов; архив,
  ключёванный по нему, при пересборке пакета сошьёт историю разных тегов.
  Требуется персистентный реестр потоков и проверка при загрузке.
- **Conformance-тесты на `IArchiveStore`**, прогон минимум на двух реализациях.
  Абстракция с единственной реализацией через год вырождается в форму этой
  реализации.
- **Тест на аварийное завершение** под нагрузкой — обязателен, не опционален.
- **Единственный писатель:** каталог архива открывается исключительно, через
  файл блокировки. Два экземпляра службы на один каталог — типичная ошибка
  развёртывания, дающая неремонтируемое повреждение.
- **Пределы запроса на чтение проверяются на сервере** (`MaxPointsPerQuery`,
  `MaxStreamsPerQuery`, `QueryTimeoutMs`). Сервер общий для всех АРМов (§5.1):
  без пределов один запрос сырого года по сотне тегов выводит из строя все
  рабочие места. При превышении ответ прореживается автоматически с признаком
  для клиента, а не отклоняется — оператор, запросивший год, хочет увидеть год.

---

## 9. Оперативное хранилище (TagTable)

Realtime-БД текущих значений в памяти. Один экземпляр на систему.

### 9.1 Синхронизация: seqlock

Между потоками драйверов (запись) и UI (чтение) используется **seqlock** —
счётчик версии на тег: нечётное значение = запись в процессе, чётное = данные целостны.

Обоснование: `TagValue` — 24 байта, её запись **не атомарна**, возможно
чтение «порванной» структуры. Блокировка на каждый тег при 10 000 тегов × 10 Гц
даёт 100 000 захватов блокировки в секунду — недопустимо. Seqlock даёт
свободное от блокировок чтение при том, что записи редки относительно чтений.

### 9.2 Обновление UI

**Не событие на каждый изменившийся тег.** Вместо этого:

1. `DispatcherTimer` с периодом 100 мс.
2. Забирает только теги, чья версия выросла с прошлого кадра («эпоха изменений»).
3. Обновляет только элементы, **реально видимые** на экране.

Это даёт задержку отображения ≤ 200 мс (требование §4.1) при постоянной
нагрузке на UI-поток независимо от количества тегов.

---

## 10. Мнемосхемы

### 10.1 Рендеринг

**Не дерево из тысяч контролов Avalonia.** Один кастомный `Control` с
`ICustomDrawOperation`, рисующий всю схему напрямую в `SKCanvas`.

- Статика (фон, трубопроводы, надписи, рамки) кэшируется в `SKPicture` и
  перерисовывается только при изменении масштаба/позиции.
- Динамика рисуется поверх, по dirty-областям.
- Частота кадров ограничена 20–30 FPS. Глазу достаточно, экономия CPU кратная.
- Панорамирование и масштабирование — через матрицу преобразования
  (подход PanAndZoom из Core2D).

### 10.2 Модель схемы — сразу под редактирование

Критично: **нельзя** сделать runtime-модель, а потом переделывать под редактор.
С самого начала модель включает:

- стабильные GUID у каждой фигуры;
- слои и z-order;
- группировку фигур;
- сериализацию с версией формата;
- структуру, дружелюбную к undo/redo (стек команд, как в Core2D).

Фундамент закладывается в M3, UI редактора — в M6.

### 10.3 Динамизация

Правило «свойство фигуры зависит от значения тега». Минимальный набор:

| Свойство | Применение |
|---|---|
| Цвет заливки / обводки | Состояние агрегата, выход за уставку |
| Видимость | Появление предупреждающих знаков |
| Текст | Числовое значение с форматом и единицами |
| Поворот | Стрелки, положение задвижки |
| Уровень заполнения | Резервуары, бункеры |
| Положение (X/Y) | Ползунки, индикаторы |
| Мигание | Неквитированная авария |

### 10.4 Библиотека символов

Графические символы (насосы, клапаны, задвижки, ёмкости, двигатели) —
SVG-файлы, рендеринг через **Svg.Skia**. Свой парсер SVG не пишем.

### 10.5 Редактор (M6)

- Инструменты: линия, прямоугольник, эллипс, полилиния, текст, символ из библиотеки.
- Undo/redo через стек команд.
- Привязка к сетке, выравнивание, распределение.
- Панель свойств с настройкой динамизации.
- Копирование/вставка, множественное выделение.
- Предпросмотр в режиме исполнения без выхода из редактора.

---

## 11. Выражения и скриптование

### 11.1 Разделение двух видов логики

«Скрипты на мнемосхеме» — это **два механизма с противоположными требованиями**.
Объединение их в один даёт либо недостаточную производительность, либо
недостаточную выразительность.

| | **Логика отображения** | **Логика действий** |
|---|---|---|
| Примеры | Цвет насоса, текст в поле, видимость значка аварии | Клик по кнопке → запись в тег и переход на схему |
| Частота | Каждый кадр, каждый видимый элемент: **до 30 000 вычислений/с** | По событию, единицы раз в минуту |
| Требования | Ноль аллокаций, предсказуемое время, гарантия завершения | Время исполнения не критично |
| Механизм | Байткод-выражения (§11.2) | Декларативные действия (§11.3) |

### 11.2 Уровень 1: выражения, компилируемые в байткод

Для динамизации нужен не язык, а **выражения**:

```
Boiler1.Temp > Boiler1.TempHi && Pump1.Running
(Tank1.Level / Tank1.Volume) * 100
Valve1.Open ? "Открыт" : "Закрыт"
```

Редактор компилирует их в компактный байткод стековой машины. **В пакет
попадает байткод, не текст.**

**Набор инструкций:** `LoadTag(индекс)`, `LoadConst(индекс)`, арифметика,
сравнения, логические операции, `JumpIfFalse`, `CallBuiltin(id)`, `Return`.

Обязательные свойства ВМ:

- **Обратные переходы запрещены на уровне формата байткода.** Следствие: циклов
  не существует, завершение выражения гарантировано структурно. Это надёжнее
  сторожевого таймера или счётчика инструкций с прерыванием.
- **Ноль аллокаций.** Стек операндов — `stackalloc Span<double>`, только
  значимые типы.
- Объём реализации — порядка 500–800 строк. Это не разработка языка.

**Обработка недостоверного качества.** Семантика `Temp > 80` при качестве
`Bad` задаётся явными встроенными функциями `IsGood(tag)`, `ValueOr(tag, default)`,
а не неявным распространением недостоверности. Обоснование: предсказуемость
для интегратора. Без явного решения этого вопроса возможна ситуация «зелёный
насос при оборванной связи».

### 11.3 Уровень 2: декларативные действия

Для событий (клик, срабатывание сигнализации, открытие схемы) язык не требуется —
достаточно **списка команд из фиксированного набора**:

```
OnClick:
  - Confirm("Запустить насос №1?")
  - WriteTag(Pump1.Cmd, 1)
  - OpenScheme("boiler-1")
```

Состав набора: `WriteTag`, `ToggleTag`, `OpenScheme`, `ShowDialog`, `Confirm`,
`AckAlarm`, `RunReport`, `Login`. Условия задаются выражениями §11.2.

Полностью валидируется в редакторе, безопасен по построению, не требует
ни компилятора, ни интерпретатора в исполнительной поставке.

### 11.4 Уровень 3: модули на C# — точка расширения

На случай, когда уровней 1–2 недостаточно: редактор компилирует C#-код проекта
через Roslyn в сборку, пакет несёт `Project.Scripts.dll`, runtime загружает её
через `AssemblyLoadContext`.

Совпадение с §5.4: **Roslyn требуется только в инженерной поставке**, у заказчика
компилятора нет — только готовая сборка.

Ограничения, которые необходимо учитывать:

- **Конфликт с NativeAOT.** AOT-приложение не может загружать IL. При выборе
  NativeAOT в M8 уровень 3 исключается. ReadyToRun с ним совместим.
- **Изоляция обязательна.** Обработка исключений по периметру, таймаут
  исполнения, правило «отключить сбойный модуль, записать в журнал, продолжить
  работу». Остановка производства из-за ошибки в скрипте недопустима.
- Версионирование сборки относительно хоста.

**Решение: точка расширения закладывается, реализация откладывается (не ранее M7).**
Уровни 1–2 покрывают подавляющее большинство задач; преждевременное открытие
уровня 3 ведёт к переносу в C# логики, которой там быть не должно.

### 11.5 Отклонённые варианты

| Вариант | Причина отклонения |
|---|---|
| Встраиваемый интерпретатор (Lua/MoonSharp, JavaScript/Jint) | Интерпретация медленнее байткодной ВМ на порядок и более. На 30 000 вычислений/с это переходит из микросекунд в заметные проценты CPU на двухъядерной машине. Дополнительно: нагрузка на GC, лишняя зависимость, ещё один язык для интегратора. |
| Хранение выражений текстом с разбором в рантайме | Разбор при загрузке расходует бюджет старта (§4.1); позднее связывание имён тегов исключает оптимизацию §11.6. |

### 11.6 Раннее связывание тегов

Главная выгода компилированного пакета — не защита от правки, а следующее.

При компиляции имя тега `Boiler1.Temp` заменяется на **индекс в `TagTable`**.
В рантайме инструкция `LoadTag` — прямое обращение к элементу массива:
без поиска по словарю, хеширования строки и разбора имени.

При 30 000 вычислений в секунду разница между обращением по индексу и поиском
строки в словаре определяет, укладываемся ли мы в бюджеты §4.1 на слабом ПК.

Побочный эффект: **несуществующий тег становится ошибкой сборки пакета**,
а не отказом на объекте.

### 11.7 Пересчёт по эпохам

Выражение вычисляется только при изменении хотя бы одного входящего в него тега.

При компиляции редактор записывает в пакет список тегов каждого выражения.
В рантайме выражение хранит последнюю обработанную эпоху изменений; механизм
эпох определён в §9.2 и переиспользуется без изменений.

Поскольку одновременно меняется малая доля тегов схемы, фактическая нагрузка
оказывается кратно ниже оценки в 30 000 вычислений.

### 11.8 Расчётный бюджет

| Показатель | Оценка |
|---|---|
| Выражение из ~10 инструкций | 50–100 нс |
| 30 000 вычислений/с без учёта §11.7 | ~3 мс процессорного времени в секунду |
| Доля ядра | Доли процента |

Запас относительно §4.1 многократный. Для сравнения: интерпретатор JavaScript
на той же нагрузке израсходовал бы сотни миллисекунд в секунду.

### 11.9 Требования к редактору выражений

Поскольку компиляция перенесена в инженерную поставку, редактор обязан обеспечить:

- автодополнение имён тегов и подсветку синтаксиса;
- проверку типов и существования тегов **до** сборки пакета;
- вычисление выражения в реальном времени при подключении к работающему
  runtime — иначе диагностика на объекте сводится к угадыванию;
- обратную распаковку пакета в читаемый вид для разбора нештатных ситуаций.

---

## 12. Масштабирование на несколько станций

Клиент-серверная архитектура (§5.1) делает несколько операторских станций
**штатным сценарием, а не расширением**: дополнительный АРМ — это ещё один
клиент, подключённый к тому же серверу. Режимы развёртывания (single ARM,
мастер-АРМ, выделенный сервер, client-only АРМ) описаны в
`docs/distributed-arm-architecture.md`.

**Следствие, которое надо знать заранее:** архив живёт на **одной машине-сервере**,
а не на каждой станции. Схема хранения это учитывает — `IHistorian` существует
только на сервере, клиенты запрашивают историю через тот же gRPC-контракт.

---

## 13. Безопасность и права доступа

- Роли: Наблюдатель (только просмотр), Оператор (+ управление и квитирование),
  Инженер (+ редактирование схем и конфигурации), Администратор (+ пользователи).
- Все операции записи в устройства и квитирования пишутся в **журнал аудита**
  (SQLite, §8.7): кто, когда, какой тег, старое и новое значение.
- Пароли — хеш с солью (PBKDF2 или Argon2), не открытым текстом.
- Автоматическая блокировка сессии по бездействию (настраивается).

---

## 14. Конфигурация проекта

Существует в двух формах: **исходной** (редактируется, живёт у разработчиков) и
**скомпилированной** (передаётся заказчику).

```
исходный проект  ──[ Scada.Package.Builder ]──>  project.scadapkg
   (инженерная поставке)                          (исполнительная поставка)
```

### 14.1 Исходная форма

Каталог на диске, версионируемый в git разработчиками:

```
MyProject.scada/
├─ project.json          метаданные, версия формата, часовой пояс объекта
├─ devices.json          каналы связи и устройства
├─ tags.json             описания тегов, включая режим логирования (§8.3)
├─ alarms.json           правила сигнализации
├─ users.json            пользователи и роли
├─ schemes/
│  ├─ overview.scheme
│  └─ boiler-1.scheme
├─ scripts/              опционально, уровень 3 (§11.4)
└─ symbols/              библиотека SVG-символов
```

Сериализация — `System.Text.Json` с source-generated `JsonSerializerContext`:
без рефлексии, быстрый старт, совместимость с NativeAOT.

Все файлы конфигурации содержат **номер версии формата** для будущих миграций.

### 14.2 Скомпилированная форма: `.scadapkg`

```
project.scadapkg
├─ manifest.json         версия формата, версия проекта, контрольные суммы
├─ tags.bin              таблица тегов с готовыми индексами
├─ devices.bin           устройства + предгруппированные запросы Modbus
├─ code.bin              единый пул байткода всего проекта
│    ├─ constants        пул строк и чисел
│    ├─ expressions      таблица смещений
│    └─ bytecode         сплошной массив
├─ schemes/
│    └─ overview.bin     геометрия + ссылки на выражения по индексу
├─ symbols/              SVG-символы
└─ modules/              опционально, уровень 3 (§11.4)
     └─ Project.Scripts.dll
```

Два решения в структуре пакета неочевидны и обязательны к соблюдению:

- **Пул байткода общий на проект, а не на схему.** Выражения вида
  `Tag > HiLimit` повторяются в проекте сотни раз; при сборке они
  дедуплицируются. Экономия памяти и времени загрузки существенна.
- **Схема хранит индекс выражения в пуле, а не само выражение.** Загрузка схемы
  сводится к чтению плоских структур без разбора текста.

### 14.3 Обоснование компилируемой формы

| Эффект | Содержание |
|---|---|
| **Скорость старта** | Бюджет — 5 секунд (§4.1). Разбор JSON с описанием 10 000 тегов, построение индексов, группировка запросов Modbus и компиляция выражений выполняются на машине инженера, а не на слабом ПК заказчика. |
| **Раннее связывание** | Имена тегов заменены индексами `TagTable` (§11.6) — ключевое условие соблюдения бюджета по частоте кадров. |
| **Целостность** | Контрольные суммы выявляют повреждение файла. Оператор не изменит конфигурацию текстовым редактором. |
| **Совместимость** | Версия формата в манифесте позволяет runtime сообщить «пакет собран более новой версией редактора» вместо аварийного завершения. |

Цена решения — потеря прозрачности при разборе нештатных ситуаций на объекте.
Компенсируется требованием §11.9: редактор обязан уметь распаковать пакет
в читаемый вид.

### 14.4 Доставка и применение пакета

Требования к исполнительной поставке при получении нового пакета:

1. Проверка контрольных сумм и версии формата **до** применения.
2. Проверка совместимости версий (§14.5).
3. Сохранение предыдущего пакета.
4. Применение с перезапуском runtime.
5. **Автоматический откат к предыдущему пакету при неуспешном запуске.**

Пункт 5 обязателен: неудачное обновление конфигурации не должно приводить
к остановке производства.

### 14.5 Правило совместимости версий

Манифест содержит версию формата пакета. Runtime:

- **загружает** пакет с версией формата, равной или меньшей поддерживаемой;
- **отказывает с внятным сообщением** при большей версии, указывая требуемую
  версию исполнительной поставки;
- **никогда не пытается** интерпретировать неизвестные структуры «по возможности».

### 14.6 Развёртывание: программа и папка проекта

Разделяются две сущности с разным жизненным циклом и разными владельцами:
**программа** — версионируется, обновляется установщиком, принадлежит нам;
**папка проекта** — изменяемые данные объекта, принадлежит заказчику.

```
C:\Program Files\<Продукт>\        ← ставится setup.exe, обновляется установщиком
    Scada.Server.exe
    Scada.App.Runtime.exe

D:\Projects\Boiler-1\              ← папка проекта, на записываемом томе
    project.scadaproj              маркер: имя, версия, ссылка на пакет
    project.scadapkg               скомпилированный проект
    runtime.json                   локальные настройки: глубина, пороги, пояс
    archive/                       значения (§8.7)
    events.db                      события, алармы, аудит (SQLite)
    logs/
```

**Всё изменяемое состояние живёт под папкой проекта.** Следствия: резервная копия
объекта — копирование одной папки; архив переносится вместе с проектом без
установки чего-либо (ради этого и отказались от сервера БД); несколько проектов
на одной машине не конфликтуют.

Папка проекта **не размещается под `Program Files`** — запись туда требует прав
администратора.

**Запуск.** Установщик регистрирует расширение `.scadaproj` за исполнительной
поставкой, поэтому проект открывается двойным кликом по маркеру в своей папке —
тем же механизмом, что документ офисного пакета. Служба регистрируется как
`Scada.Server.exe --project "D:\Projects\Boiler-1"`.

Исполняемый файл **внутрь папки проекта не кладётся**. Он был бы либо второй
копией программы — с расхождением версий между установленной и лежащей в
папке, — либо лаунчером, который всё равно ищет установленную. Кроме того,
модель «двойной клик по exe в папке» не соответствует запуску сервера службой
(§5.1), которая стартует до входа пользователя.

**Выбор проекта.** Путь передан аргументом или маркером — проект открывается
молча, без диалогов; это боевой режим, автозапуск не должен ничего спрашивать.
Аргумента нет — открывается последний использованный. Диалог выбора проекта
существует, но доступен под правами инженера (§13): оператору исполнительной
поставки выбор предлагать нельзя, у него на экране его объект и ничего больше.

Клиентскому АРМу папка проекта не нужна: пакет он получает от сервера и кэширует
(M6.5). Управление папками требуется только там, где работает сервер.

---

## 15. Правила разработки

### 15.1 Настройки сборки (обязательны с самого начала)

```xml
<Nullable>enable</Nullable>
<TreatWarningsAsErrors>true</TreatWarningsAsErrors>
<EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
<PublishReadyToRun>true</PublishReadyToRun>
<TieredPGO>true</TieredPGO>
<ServerGarbageCollection>false</ServerGarbageCollection>
```

`Nullable` и `TreatWarningsAsErrors` включаются **немедленно**. Включить их
на зрелом проекте практически невозможно — тысячи предупреждений.

Версии пакетов — только через `Directory.Packages.props` (Central Package
Management). Одна версия пакета на всё решение.

### 15.2 Правила кода

- В горячих путях (опрос, TagTable, рендер кадра) запрещены: LINQ, boxing,
  замыкания с захватом, `params`-массивы, рефлексия.
- Буферы — `ArrayPool<T>`, работа с ними — `Span<T>` / `Memory<T>`.
- Весь ввод-вывод асинхронный. `async void` запрещён (кроме обработчиков событий UI).
- `CancellationToken` пробрасывается во все асинхронные методы.
- Публичные API документируются XML-комментариями. Комментарии объясняют
  **почему**, а не **что** — «что» должно быть видно из кода.
- Никаких «магических чисел» — именованные константы.

### 15.3 Тестирование

- Автотесты покрывают ядро: `TagTable` (включая многопоточность), парсеры
  протоколов, правила алармов, кодеки архива (обратимость), сериализацию.
- UI автотестами не покрывается — дорого и хрупко. Проверяется вручную.
- `Scada.Architecture.Tests` проверяет правила зависимостей §5.3.

### 15.4 Бенчмарки

BenchmarkDotNet на горячие пути **начиная с M1**, не «когда-нибудь потом»:
пропускная способность `TagTable`, парсинг кадра Modbus, время кадра рендера,
скорость сжатия архива.

Цель — ловить регрессии производительности сразу, а не обнаружить на M6, что
система не тянет.

### 15.5 Процесс работы

- Одна веха — одна ветка. Не смешивать несколько тем в одном наборе изменений.
- Каждая веха завершается **работающим приложением**, которое можно запустить.
- Перед началом вехи — краткое согласование состава работ.
- Правила проекта дублируются в `CLAUDE.md` в корне репозитория.

---

## 16. Организация работы команды

Состав: **два разработчика**. Раздел определяет зоны ответственности и порядок
взаимодействия.

### 16.1 Принцип разделения: по слоям, а не по вехам

Разделение вида «ты делаешь M2, я делаю M3» **не применяется**. M3 зависит от M1,
M4 от M2 — при таком делении один разработчик постоянно простаивает в ожидании другого.

Граница проходит по интерфейсу **`IRuntimeClient`** (§5.1). Это правильный шов:

- контракт узкий, согласуется один раз;
- по обе стороны требуются разные навыки;
- контракт можно заглушить, поэтому ни одна сторона не блокирует другую.

### 16.2 Фаза 0: совместная работа (M0 и ядро M1)

Первые 1–2 недели разработчики работают **совместно** — парно либо в режиме
«один пишет, второй немедленно читает».

Обоснование: на этом этапе формируются контракты и соглашения по коду.
Раздельная работа с самого начала приводит к двум несовместимым стилям и
расхождению в понимании доменной модели.

До разделения должны быть окончательно зафиксированы:

1. `TagValue`, `TagId`, `Quality` — модель данных (§6.1);
2. `TagDefinition` — описание тега (§6.2);
3. `IRuntimeClient` — контракт UI ↔ ядро;
4. формат файлов конфигурации и пакета `.scadapkg` (§14);
5. состав встроенных функций и семантика качества в выражениях (§11.2).

Дальнейшее изменение этих сущностей допускается **только по обоюдной
договорённости**, а не единоличным коммитом.

### 16.3 Зоны ответственности

| | **Разработчик A — Ядро и данные** | **Разработчик B — Графика и UI** |
|---|---|---|
| Проекты | `Scada.Core`, `Scada.Expressions`, `Scada.Expressions.Compiler`, `Scada.Runtime`, `Scada.Drivers.*`, `Scada.Historian`, `Scada.Alarms` (движок правил), `Scada.Package`, `Scada.Package.Builder` | `Scada.Graphics`, `Scada.Editor`, `Scada.App.Shared`, `Scada.App.Runtime`, `Scada.App.Engineering` |
| Вехи | M1 (ядро), M2, M2.5, M4 (архив), M5 (движок), M7 (запись в устройства) | M3, M4 (тренды), M5 (UI сигнализации), M6, M7 (UI прав доступа) |
| Характер работы | Многопоточность, бинарные форматы, сетевые протоколы, байткод-ВМ, оптимизация | SkiaSharp, композиция, эргономика оператора, undo/redo |
| Бенчмарки | Пропускная способность `TagTable`, сжатие архива, исполнение выражений | Время кадра рендера |

Распределение ролей — по профессиональной склонности. Объём работ сопоставим:
у A два протокола, архив и подсистема выражений, у B редактор мнемосхем
(M6 — самая объёмная веха проекта).

**Разграничение по выражениям (§11):** A владеет байткодом, ВМ и компилятором
(язык, набор инструкций, семантика). B владеет UI редактора выражений
(§11.9: автодополнение, подсветка, отображение ошибок). Граница — публичный API
`Scada.Expressions.Compiler`. Согласуется в M1 вместе с остальными контрактами.

### 16.4 Заглушки, обеспечивающие независимость

Реализуются в M1. Без них разработчик B простаивает значительную часть времени.

| Заглушка | Назначение |
|---|---|
| **Драйвер-симулятор** (`Scada.Drivers.Simulator`) | Генерирует 10 000–20 000 правдоподобных тегов: синусоиды, ступени, шум, имитация обрывов связи. Позволяет B разрабатывать схемы и тренды без контроллера и без готового драйвера Modbus. |
| **`InMemoryHistorian`** | Кольцевой буфер на последний час. Позволяет B разрабатывать тренды до готовности реального формата архива. |

**Уточнение по итогам проектирования M4:** `InMemoryHistorian` не заменяется, а
**становится штатным узлом** — кольцевым буфером внутри фасада `IHistorian`
(`docs/archive-format.md` §13.4). Realtime-тренды и правила сигнализации
обслуживаются им и диска не касаются. Заглушкой на время разработки становится
`InMemoryArchiveStore` — реализация шва, она же вторая реализация для
conformance-тестов.

### 16.5 Параллельные треки

| Период | Разработчик A | Разработчик B |
|---|---|---|
| 1 | **M0 + ядро M1 — совместно** | |
| 2 | M1: `TagTable`, движок опроса, бенчмарк | M1: shell, страница тегов, симулятор |
| 3 | M2: Modbus TCP/RTU | M3: модель схемы, Skia-рендерер |
| 4 | M4: формат архива, сжатие, агрегаты | M3: динамизация, SVG-символы, pan/zoom |
| 5 | M5: движок правил сигнализации | M4: тренды на ScottPlot |
| 6 | M2.5 / оптимизация / M7 | M6: редактор мнемосхем |
| 7 | Поддержка M6 со стороны данных, M7 | M6: редактор (продолжение) |
| 8 | **M8 — совместно: профилирование, установщик** | |

Длительная работа B над M6 — штатная ситуация: A в этот период закрывает архив,
сигнализацию и управление.

Подсистема выражений и сборка пакета (§11, §14.2) выполняются разработчиком A
в составе M1 и M3: они являются входными данными для схем, поэтому должны быть
готовы к моменту, когда B приступает к динамизации.

### 16.6 Порядок работы с репозиторием

- Ветка `main` защищена, слияние **только через pull request**.
- Именование веток: `feature/m2-modbus-tcp`, `feature/m3-scheme-renderer`.
- **Взаимный ревью обязателен.** При двух разработчиках это единственный барьер
  против накопления технического долга. Ревью содержательный, не формальный.
- **PR дробятся.** Веха одним PR на несколько тысяч строк не поддаётся ревью.
  Ориентир: M3 — это 4–5 отдельных PR (модель, рендерер, динамизация, символы, pan/zoom).
- CI на каждый PR: сборка без предупреждений, прогон тестов, проверка
  архитектурных правил (`Scada.Architecture.Tests`).

### 16.7 Работы, выполняемые совместно

Независимо от зон ответственности, совместно проектируются и реализуются два узла,
где цена ошибки максимальна, а последующее исправление наиболее затратно:

1. **`TagTable` на seqlock** (§9.1). Ошибки многопоточности не выявляются
   ревью по диффу. Требуются две пары глаз и отдельный стресс-тест.
2. **Формат архива** (§8.7). Переделка после накопления данных за месяцы
   практически невыполнима. Проектируется совместно на бумаге **до начала
   реализации** — `docs/archive-format.md`. Кодирование не начинается, пока
   спецификация не просмотрена обоими разработчиками.

По завершении каждой вехи — взаимный разбор сделанного. Требование: каждый
разработчик должен в общих чертах понимать зону ответственности коллеги, чтобы
отсутствие одного не останавливало работу.

### 16.8 Синхронизация

- Ежедневно, кратко: текущая задача, возникшие затруднения.
- Еженедельно: сверка с настоящим ТЗ — какие пункты закрыты, есть ли движение
  по открытым вопросам (§18).
- **Настоящий документ обновляется при каждом изменении принятых решений.**
  ТЗ, разошедшееся с кодом, теряет ценность и начинает вводить в заблуждение.

### 16.9 Известные риски совместной работы

| Риск | Мера |
|---|---|
| Конфликты слияния в `.sln` и `.csproj` | Central Package Management (§15.1) выносит версии пакетов в один файл и снижает частоту конфликтов |
| Расхождение стиля кода | `.editorconfig` + `TreatWarningsAsErrors` + дисциплина ревью |
| Пересечения в `Scada.App` (A добавляет страницы диагностики каналов) | Правка файла из чужой зоны — с предварительным уведомлением. Систематические правки одних и тех же файлов обоими — признак неверно проведённой границы, требует пересмотра зон |
| B опережает A и упирается в неготовое ядро | Заглушки §16.4 |

---

## 17. Дорожная карта

Исполнители указаны согласно зонам ответственности §16.3. M0 и M1 выполняются
совместно (§16.2) — на них формируются контракты, разделение зон начинается с M2.

### M0 — Фундамент — *совместно*

Структура решения по §5.2, `Directory.Build.props`, `Directory.Packages.props`,
`.editorconfig`, `CLAUDE.md`, DI через `Microsoft.Extensions.Hosting`,
логирование, главное окно с навигацией.

**Разделение поставок закладывается здесь** (§5.4): создаются `Scada.App.Shared`,
`Scada.App.Runtime` и `Scada.App.Engineering`, включая автотест запрета ссылок
на инструментальные сборки из исполнительной поставки.

**Критерий приёмки:** обе поставки запускаются, показывают shell с пустыми
страницами, решение собирается без предупреждений, автотест правил
зависимостей проходит.

### M1 — Ядро реального времени — *совместно*

`TagTable` на seqlock, `TagDefinition` и загрузчик конфигурации,
драйвер-симулятор, движок опроса, `IRuntimeClient` + `LocalRuntimeClient`,
`InMemoryHistorian` (заглушка, §16.4), страница «Теги» с живыми значениями.

Подсистема выражений (§11.2): набор инструкций, ВМ, компилятор, встроенные
функции, семантика качества. Первичная версия `Scada.Package.Builder` и
`Scada.Package` — чтобы схемы в M3 сразу разрабатывались на пакете, а не
на исходных файлах.

**Критерий приёмки:** бенчмарк на **20 000 тегов** укладывается в бюджеты §4.1
(RAM ≤ 500 МБ, CPU ≤ 25 %, задержка ≤ 200 мс). Исполнение выражения из
10 инструкций — не более 100 нс без аллокаций (§11.8). Формат архива описан
на бумаге.

Это контрольная точка архитектуры: если бюджеты не держатся, правим здесь,
пока это дёшево.

### M2 — Драйвер Modbus — *исполнитель: A*

Modbus TCP и RTU, группировка запросов, переподключение, таймауты,
диагностические теги канала.

**Критерий приёмки:** чтение реального контроллера LicOS, устойчивая работа
при обрыве и восстановлении связи.

### M2.5 — OPC UA — *исполнитель: A* *(только при подтверждении необходимости)*

### M3 — Мнемосхемы (режим исполнения) — *исполнитель: B*

Модель схемы под редактирование (§10.2), Skia-рендерер, динамизация через
выражения (§11.2), декларативные действия (§11.3), SVG-символы, панорамирование
и масштабирование.

**Критерий приёмки:** 30 FPS при 500 динамических элементах на целевом железе.
Схема загружается из `.scadapkg`; пересчёт выражений выполняется по эпохам (§11.7).

### M4 — Архив и тренды — *исполнители: A (архив) + B (тренды)*

Состав со стороны архива, в порядке выполнения:

1. Спецификация формата на бумаге — `docs/archive-format.md`, просмотр обоими
   разработчиками (§16.7). **Сделано до начала кодирования.**
2. Правки модели данных: убрать `Deadband`, добавить режимы логирования с
   умолчанием по `DataType`. Меняет `TagDefinition` → по §16.2 согласуется.
3. Битовый поток, кодеки (разность разностей, XOR), тесты обратимости.
4. Контракты: `IHistorian` (фасад чтения, `TagId`), `IArchiveStore` (шов,
   `streamId`), зеркало в `IRuntimeClient`. Conformance-тесты, прогон на двух
   реализациях. **Согласуется с B до кодирования** — тренды пишутся против этих
   интерфейсов параллельно (§16.1).
5. Конвейер `ArchivePipeline` как `BackgroundService`: режимы логирования,
   качество, монотонность, представление значения, реестр потоков, кольцевой
   буфер. Записи извне нет — конвейер читает `TagTable` сам.
6. Файловый стор: журнал упреждающей записи, CRC, восстановление, тест на
   аварийное завершение.
7. Ротация через `IRetentionPolicy` (§8.6), лестница при нехватке места (§8.9),
   диагностические теги архива (§7.5). Калькулятор объёма архива в
   `Scada.Package.Builder`.

Тренды на ScottPlot: realtime и исторический режим, курсор, экспорт CSV.
Читатель обязан различать семантику пропуска по режиму логирования (§8.3):
разрыв линии при Periodic, удержание при OnChange.

**Критерии приёмки:**

1. Открытие тренда 10 тегов за 8 часов ≤ 1 с, за год ≤ 2 с (§4.1).
2. Суточный прогон симулятора: фактический рост архива совпадает с
   предсказанием калькулятора объёма по фактической конфигурации в пределах
   **25 %**. Критерий самосогласован — проверяем при любом исходе и заодно
   проверяет калькулятор. Абсолютная оценка §8.5 для приёмки не используется:
   она получена расчётом, а не замером.
3. Аварийное завершение процесса под нагрузкой: архив открывается, порванный
   хвост обрезан, потеря не превышает 10 секунд (§4.2).
4. Conformance-набор проходит на файловой и in-memory реализациях.
5. Бенчмарки (§15.4): пропускная способность кодека на запись и на чтение,
   отсутствие аллокаций в установившемся режиме конвейера.

### M5 — Сигнализация — *исполнители: A (движок правил) + B (UI и журнал)*

Правила HiHi/Hi/Lo/LoLo, дискретные, отклонение от уставки, скорость изменения.
Квитирование, звуковая сигнализация, журнал событий с фильтрами.
Хранилище событий, алармов и аудита на SQLite (§8.7).

Гистерезис правил (чтобы аларм не дребезжал на границе уставки) задаётся **в
правиле**, а не на теге: это свойство условия, а не измерения.

### M6 — Редактор мнемосхем — *исполнитель: B*

Инструменты, undo/redo, сетка и привязки, библиотека символов, панель свойств,
настройка динамизации, редактор выражений по §11.9 (автодополнение, подсветка,
проверка до сборки, вычисление в реальном времени).

Самая объёмная веха проекта. Параллельно A закрывает M4, M5 и M7 со стороны данных.

### M6.5 — Сборка и доставка пакета — *исполнитель: A*

Завершение `Scada.Package.Builder`: полная валидация проекта, дедупликация пула
байткода, контрольные суммы. В исполнительной поставке — приём пакета,
проверка совместимости версий (§14.5), применение и **автоматический откат**
при неуспешном запуске (§14.4).

**Критерий приёмки:** неудачное обновление пакета не приводит к неработоспособности
системы — выполняется откат к предыдущей версии.

### M7 — Управление и права — *исполнители: A (запись в устройства) + B (UI прав доступа)*

Запись в устройства с подтверждением, роли и права доступа, журнал аудита.
Диагностика в исполнительной поставке по §5.4.2.

### M8 — Полировка и выпуск — *совместно*

Профилирование на целевом железе, ReadyToRun (опционально NativeAOT),
**два установщика** — инженерный и исполнительный, документация оператора
и инженера.

При выборе NativeAOT — окончательный отказ от уровня 3 скриптования (§11.4).

---

## 18. Открытые вопросы

Ведутся до закрытия. Ни один из них не блокирует M0 и M1.

| № | Вопрос | Нужен к вехе | Статус |
|---|---|---|---|
| 1 | Конкретная серия контроллера LicOS и карта регистров Modbus | M2 | **Открыт.** Заказчик не знает. Запросить у поставщика оборудования. |
| 2 | Детальность годовой истории: часовые агрегаты за год достаточно, или нужны посекундные данные за любой день? | M4 | **Закрыт 2026-08-14.** Вопрос снят конструкцией: хранятся сырые значения за весь срок, доступны любые данные за любой день (§8.1). Ответ заказчика не требуется. |
| 3 | Количество операторских станций | M7 | **Открыт.** Архитектурная готовность обеспечена (§12), реализация отложена. |
| 4 | Требуется ли OPC UA дополнительно к Modbus | M2.5 | **Открыт.** Зависит от ответа на вопрос 1. |
| 5 | Требуется ли MQTT (наличие брокера у заказчика) | после M2 | **Открыт.** |
| 6 | **Кто вносит изменения в проект после сдачи?** Только разработчики, инженер заказчика, или система передаётся на сопровождение | M6.5 | **Открыт.** Определяет, передаётся ли заказчику инженерная поставка вообще, и не превращается ли задача из «разделить поставки» (§5.4) в «ограничить права» (§13). |
| 7 | Требуется ли защита исполнительной поставки от развёртывания на других объектах | M8 | **Открыт.** Бизнес-вопрос. Технические варианты: лицензионный файл, привязка к оборудованию, ограничение по числу тегов. Реализация не планируется до появления требования. |
| 8 | Семантика недостоверного качества в выражениях и окончательный состав встроенных функций | M1 | **Открыт.** Принято предварительное решение (§11.2): явные `IsGood`, `ValueOr` вместо неявного распространения недостоверности. Требует подтверждения до фиксации формата байткода. |

**Принятые проектные допущения при отсутствии ответов:**

- Количество тегов: 10 000 подтверждено, проектный запас — 20 000.
- Частота опроса: до 10 Гц для критичных тегов, настраивается на устройство.
- Основной протокол: Modbus TCP/RTU.
- Политика хранения: сырые значения, `RetentionDays = 400` (§8.6).
- Заказчику передаётся **только исполнительная поставка** (§5.4).
- Уровень 3 скриптования (§11.4) не реализуется; закладывается точка расширения.

---

## 19. Референсы и лицензии

Изучены как источники решений:

| Проект | Что заимствуется |
|---|---|
| [RapidScada v6](https://github.com/RapidScada/scada-v6) | Доменная модель (канал = значение + качество), режимы логирования (по изменению / периодический / по расписанию), структура конфигурации проекта. Разделение архива на минутный и часовой **не заимствовано** — обоснование в §8.1. Код не переносится — завязан на WinForms и тяжеловесен. |
| [Core2D](https://github.com/wieslawsoltes/Core2D) | Подход к редактору: стек команд undo/redo, абстракция рендерера, PanAndZoom-контейнер, модель «фигура как ViewModel». |
| [Svg.Skia](https://github.com/wieslawsoltes/Svg.Skia) | Используется как библиотека для рендеринга SVG-символов. |
| [ScottPlot](https://github.com/ScottPlot/ScottPlot) | Используется как библиотека для трендов. |
| [LiveCharts2](https://github.com/Live-Charts/LiveCharts2) | Резервный вариант для графиков. |

> **Требование:** перед переносом **любого кода** (не идей) из перечисленных
> проектов необходимо проверить их лицензии на совместимость с условиями
> поставки нашей системы. Заимствование архитектурных решений и подходов
> ограничений не имеет; копирование исходников — имеет.

---

## 20. История изменений

| Версия | Дата | Изменения |
|---|---|---|
| 1.0 | 2026-08-06 | Первая редакция по результатам обсуждения требований |
| 1.1 | 2026-08-06 | Добавлен раздел «Организация работы команды» (два разработчика: зоны ответственности, заглушки, порядок ревью). В дорожную карту добавлены исполнители по вехам. |
| 1.2 | 2026-08-06 | **Разделение поставок** на инженерную и исполнительную (новый §5.4), с обоснованием и разбором отклонённых вариантов. **Новый §11 «Выражения и скриптование»**: трёхуровневая модель (байткод-выражения, декларативные действия, точка расширения на C#), раннее связывание, пересчёт по эпохам. §14 переработан под **компилируемый пакет `.scadapkg`**: формат, обоснование, доставка с откатом, правило совместимости версий. Обновлены §5.2 и §5.3 (новые проекты и запреты), §16.3 (зоны ответственности), дорожная карта (M0, M1, M3, M6, новая M6.5, M8). Добавлены открытые вопросы 6–8. Разделы 11–19 перенумерованы в 12–20. |
| 1.4 | 2026-08-14 | **§5.3 приведён в соответствие с фактической раскладкой**: `Scada.Historian` находится ниже `Scada.Runtime`, а не выше — разделение прошло по границе «формат против интеграции». Добавлен запрет на ссылки рантайма на конкретные драйверы и зафиксировано известное нарушение (симулятор в `DriverFactory`), обнаруженное только потому, что `Scada.Architecture.Tests` не существует. **Расписание логирования считается в местном времени объекта** (§8.3): добавлен `SiteTimeZone`, переходы на летнее время разрешены явно, LINQ из горячего пути убран. **Квантование по `Precision` реализовано** (§8.2): замер показал, что XOR на вычисляемых double раздувает данные до 8,4 байта на отсчёт при 8 байтах несжатого значения, квантование даёт выигрыш 10,8×; константы калькулятора объёма исправлены и удерживаются тестом калибровки. **Ротация архива, `IRetentionPolicy` и лестница при нехватке места** реализованы (§8.6, §8.9). **Контракты чтения истории** (`docs/archive-format.md` §13): `IHistorian` перепроектирован в фасад чтения — `Append` удалён, добавлены чтение бакетами, якорь на левом краю тренда и синхронное чтение из кольца; конвейер сделан `BackgroundService`, который читает `TagTable` сам, публичной записи в архив не существует. `InMemoryHistorian` из заглушки становится штатным узлом (§16.4). Бенчмарки архива внесены в критерии приёмки M4. **Правила записи и отказоустойчивость архива:** в архив пишется только достоверное качество, одна точка на переходе в `!Good` (§8.4); строгая монотонность меток времени с отбрасыванием нарушителей (§8.4, требование вскрыто поведением `PollingEngine.MarkTagsBad`); лестница при нехватке места с полом `MinRetentionDays` (§8.9); единственный писатель через файл блокировки и пределы запроса на чтение (§8.10). **Диагностика архива** — новый §7.5. **Развёртывание** — новый §14.6: программа ставится установщиком, изменяемое состояние живёт в папке проекта, запуск по ассоциации `.scadaproj`, выбор проекта под правами инженера. **`TagValue` приведён в соответствие с кодом** (§6.1): Unix-миллисекунды вместо тиков, поле `TagFlags` отсутствует. В §6.2 добавлены `IsArchived` и `Precision`. **Глубина хранения — параметр проекта, а не свойство системы** (§1.1, §8.6): система разрабатывается как переиспользуемый продукт, срок настраивается, формат ограничений не накладывает; срок разрешается через `IRetentionPolicy`, часовая свёртка вводится при глубине более 2–3 лет. §4.3 переформулирован из константы в расчёт на проект. **Пересмотр §8 «Историзация» целиком** по результатам разбора расчётов. Исходная схема перемножала выигрыш апертурного отбора и выигрыш сжатия, хотя оба расходуют одну и ту же избыточность — из-за завышенной оценки была спроектирована трёхуровневая схема хранения, решающая несуществующую проблему места. **Один уровень — сырые значения за весь срок**, вместо L0/L1/L2 (§8.1). **Апертурный отбор (deadband) и heartbeat убраны** (§8.2), вместо них представление значения по решётке `ScaleFactor` и режимы логирования, выводимые из `DataType` (§8.3). **Агрегаты — summary-заголовки блоков ценой 0 байт** вместо уровня хранения (§8.4). **Заморозка вокруг аварий и политики на группу тегов удалены** (§8.6): в пределах срока хранения ничего не удаляется. **PostgreSQL + TimescaleDB отклонён** (§3.1, §8.7), архив значений — собственный формат, события и аудит — SQLite. Добавлена спецификация `docs/archive-format.md` как часть ТЗ, долг §8.8 закрыт. Обновлены §2 (термины), §3, §4.1 (бюджет RAM мастер-АРМа, тренд за год), §4.3 (100 ГБ), §6.2, §13, §14.1, §16.7, §17 (M4 переписана, критерии приёмки самосогласованы; M5), §19. Открытый вопрос 2 закрыт конструкцией. |
| 1.3 | 2026-08-07 | **БД истории и событий**: PostgreSQL + TimescaleDB вместо SQLite. Убраны все упоминания SQLite. Добавлены `Npgsql` и `Npgsql.EntityFrameworkCore.PostgreSQL` в технологический стек (§3). **Мастер-АРМ**: уточнено, что мастер-АРМ / сервер обычно мощнее АРМа оператора (§1.1). **Логирование**: уточнено, что система опрашивает 10 000+ тегов, но в архив пишется 10–30 % (§1.1, §8.1, §8.4). **Бинарный формат**: сохранён как fallback / оптимизация (§8.7, новый §8.9). **RAM**: добавлен отдельный бюджет для мастер-АРМа с PostgreSQL (§4.1). **Журнал аудита**: перенесён в PostgreSQL (§13). |
